spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/pushnotification
    username: postgres
    password: postgres
server:
  port: 8083